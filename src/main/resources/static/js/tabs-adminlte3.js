function addTabMenu( menuId, menuNm ) {

    //$('#home-tab-menu').

    var tab = "";
    tab += "<li class='nav-item'>";
    tab += "  <a class='nav-link active' id='menuId' data-toggle='pill' href='#' role='tab' aria-controls='custom-tabs-one-home' aria-selected='true'>"
    tab += menuNm;
    tab += "  </a>";
    tab += "</li>";
}

function deleteTabMenu( menuId, menuNm ) {
    
}