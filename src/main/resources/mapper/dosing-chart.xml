<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pharm.chorok.web.main.repository.DosingRepository">

    <insert id="insertTbPpDosgChart" parameterType="com.pharm.chorok.domain.table.TbPpCnstChart">
	<![CDATA[    
    INSERT INTO TB_PP_DOSG_CHART (
          CNST_ID
        , DOSG_DT
        , DOSG_TP_CD
        , CALL_YN
        , REG_DT
        , REG_USR_NO
    ) 
    SELECT #{cnstId}
         , BASE_DT_STR
         , DOSG_TP_CD
         , 'N'
         , now()
         , #{regUsrNo}
      FROM (
            SELECT A.ROWNUM
                 , CASE WHEN ROWNUM  = 0  THEN  '0'
                        WHEN ROWNUM  <= 3  THEN '1'
                        WHEN ROWNUM  <= 7  THEN '2'
                        WHEN ROWNUM  <= 14 THEN '3'
                        WHEN ROWNUM  <= 21 THEN '4'
                        WHEN ROWNUM  <= 28 THEN '5'
                END AS DOSG_TP_CD
                , A.BASE_DT_STR
            FROM (
                SELECT CAL.*
                        , @ROWNUM := @ROWNUM + 1 AS ROWNUM
                    FROM bazzarmall.TB_COMM_CALENDAR CAL
                        , (SELECT @ROWNUM :=-1) TMP
                    WHERE 1 = 1
                    AND BASE_DT_STR >= DATE_FORMAT( DATE_ADD( NOW(), INTERVAL -1 DAY), '%Y%m%d')
                    AND BASE_DT_STR < DATE_ADD( NOW(), INTERVAL 27 DAY)
                ) A
            ORDER
                BY A.ROWNUM
            ) A
	]]>
	</insert>



    <select id="selectDosingChartByCustId" parameterType="map" resultType="com.pharm.chorok.domain.main.ResultDosingVo">
	<![CDATA[    
    SELECT @ROWNUM := @ROWNUM + 1 AS SEQ
         , CONCAT(@ROWNUM, '일차') AS SEQ_STR
         , DOSG_ID
         , CNST_ID
         , DATE_FORMAT(DOSG_DT, '%Y-%m-%d') AS DOSG_DT
         , (SELECT DAYS_STR_KOR FROM TB_COMM_CALENDAR CAL WHERE CAL.BASE_DT_STR = DATE_FORMAT(DOSG_DT, '%Y%m%d')) AS DAYS_STR_KOR
         , DOSG_TP_CD
         , CALL_YN          
         , DOSG_YN          
         , CURR_WGT    
         , LOSS_WGT     
         , RMI_WGT
         , DOSG_DESC1       
         , DOSG_DESC2       
         , REG_DT           
         , REG_USR_NO
      FROM TB_PP_DOSG_CHART DOSG
         , (SELECT @ROWNUM :=-1) TMP
     WHERE CNST_ID = #{cnstId}
     ORDER
        BY DOSG_ID
	]]>
    </select>

    <insert id="updateNewDosingChart" parameterType="com.pharm.chorok.domain.table.TbPpDosgChart">
	<![CDATA[    
    INSERT INTO TB_PP_DOSG_CHART (
          CNST_ID
        , DOSG_DT
        , DOSG_TP_CD
        , CALL_YN
        , DOSG_YN
        , CURR_WGT
        , LOSS_WGT
        , RMI_WGT
        , DOSG_DESC1
        , DOSG_DESC2
        , REG_DT
        , REG_USR_NO
    ) VALUES (
          #{cnstId}
        , #{dosgDt}
        , #{dosgTpCd}
        , #{callYn}
        , #{dosgYn}
        , #{currWgt}
        , #{rmiWgt}
        , #{dosgDesc1}
        , #{dosgDesc2}
        , #{regDt}
        , #{regUsrNo}
    )
	]]>
	</insert>

    <update id="updatetNewDosingChart" parameterType="com.pharm.chorok.domain.table.TbPpDosgChart">
	<![CDATA[    
    UPDATE TB_PP_DOSG_CHART 
       SET UPD_DT      = #{regDt} 
         , UPD_USR_NO  = #{regUsrNo} 
         , DOSG_DT     = #{dosgDt}
         , DOSG_TP_CD  = #{dosgTpCd}
         , CALL_YN     = #{callYn}
         , DOSG_YN     = #{dosgYn}
         , CUST_WGT    = #{custWgt} 
         , LOSS_WGT    = #{lossWgt}
         , RMI_WGT     = #{rmiWgt}
         , DOSG_DESC1  = #{dosgDesc1} 
         , DOSG_DESC2  = #{dosgDesc2} 
     WHERE DOSG_ID = #{dosgId}
	]]>
	</update>

</mapper>